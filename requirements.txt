# Rinon Voice Lab has no direct third-party Python package requirements.
# The app server uses only Python standard library modules.
#
# Irodori-TTS is installed into its own virtual environment by:
#   start_chat_uv.bat
# or:
#   powershell -ExecutionPolicy Bypass -File tools\install_irodori_tts.ps1
#
# Do not expect `pip install -r requirements.txt` to install Irodori-TTS.
